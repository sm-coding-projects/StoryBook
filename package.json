{
  "name": "client-gallery-suite",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "supabase db push --local",
    "db:seed": "tsx infra/scripts/seed.ts",
    "setup": "pnpm install && pnpm run setup:infra",
    "setup:infra": "tsx infra/scripts/setup.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
